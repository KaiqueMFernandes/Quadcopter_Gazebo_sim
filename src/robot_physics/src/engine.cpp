#include "engine.hpp"

namespace modular_robot {

// Implementation for PhysicsCore methods if needed (currently all inline)

// Example: You can move computeMotorForces, setMotorSpeeds, updateMotorControllers here if you want out-of-line definitions.

// For now, all logic is in the header for simplicity.
}